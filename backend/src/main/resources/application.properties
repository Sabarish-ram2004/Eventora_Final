spring.application.name=eventora-backend
server.port=${PORT:8080}

# SUPABASE CLOUD DATABASE
spring.datasource.url=jdbc:postgresql://aws-1-ap-northeast-1.pooler.supabase.com:6543/postgres
spring.datasource.username=postgres.fdzpluyytdoydnteqxbt
spring.datasource.password=Sabarish@0109
spring.datasource.driver-class-name=org.postgresql.Driver

# SSL (VERY IMPORTANT FOR SUPABASE)
spring.datasource.hikari.data-source-properties.ssl=true
spring.datasource.hikari.data-source-properties.sslmode=require

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.format_sql=true

# REDIS CONFIG
spring.data.redis.host=localhost
spring.data.redis.port=6379
spring.data.redis.timeout=2000ms

# SMTP CONFIG
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=syncoraevents@gmail.com
spring.mail.password=qguzoqxonctethxp
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# JWT
jwt.secret=eventoraSecretKey2024SuperSecureKeyAtLeast256BitsLong!@#$%
jwt.expiration=86400000
jwt.refresh-expiration=604800000

# OTP Config
otp.expiry.minutes=5
otp.length=6

# File Upload
spring.servlet.multipart.enabled=true
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=50MB
file.upload.dir=${UPLOAD_DIR:uploads}

# AI Microservice
ai.service.url=${AI_SERVICE_URL:http://localhost:8000}

# CORS
cors.allowed.origins=${CORS_ORIGINS:http://localhost:5173,http://localhost:3000}

# Actuator
management.endpoints.web.exposure.include=health,info,metrics
management.endpoint.health.show-details=when_authorized

# Logging
logging.level.com.eventora=INFO
logging.level.org.springframework.security=WARN
